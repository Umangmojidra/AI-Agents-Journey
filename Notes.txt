Day-01
You now understand the two most important agent concepts:
✅ Tool descriptions drive Claude's decisions — not your code
✅ The agent loop — Claude runs until end_turn, calling tools as needed
✅ Parallel tool calls — Claude optimises automatically when tools are independent

Day-02 : strategies + token-aware smart agent

Que: What happens when the messages list grows huge after 50 turns?
Ans:
1. Summarisation — periodically compress old messages into a short summary, replace them with that summary, keep going
2. Sliding window — keep only the last N messages, drop the oldest
3. Scratchpad — give Claude a dedicated thinking space inside tool results so it doesn't need to re-read the entire history.

Learned:

Sliding window — fast, loses old context, right for short tasks
Summarisation — preserves meaning, costs one extra API call, right for long sessions
Scratchpad — Claude thinks before acting, reduces errors in complex tasks
Token counting — check before every API call, trigger summarisation automatically
The tool_use / tool_result pairing rule — every tool call needs a result before next API call

Day-03 
One agent, all tools → faster, cheaper, gets confused on complex tasks
Coordinator + subagents → slower, costs more, handles complex tasks reliably


        USER
         |
         | gives task
         ↓
COORDINATOR AGENT (Claude)
  |         |          |
  |         |          |
  ↓         ↓          ↓
Search    Calculator  Writer
Agent     Agent       Agent
(Claude)  (Claude)    (Claude)
  |         |          |
  |         |          |
  ↓         ↓          ↓
Web API   eval()    Formats
                    report
  |         |          |
  └─────────┴──────────┘
            |
            ↓
        Final answer
          to USER

Day - 04 
MCP is a protocol. Not a library, not a framework — a protocol.
Like HTTP is a protocol for web requests, MCP is a protocol for connecting AI agents to external tools. 
It defines exactly how a client (your agent) talks to a server (the tool provider) so both sides know what to expect.